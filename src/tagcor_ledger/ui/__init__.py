"""PySide6 介面：頁面、共用 widget 與 controller。

**是 PySide6，不是 PyQt。** 這份 docstring 曾經寫著「PyQt UI package」——
而 `AGENTS.md` 的「不做的事」明文禁止重新加入 PyQt6，理由見
`docs/decisions/ADR-0009-keep-pyside6.md`。

**只有這一層碰得到 Qt，而且不得直接撰寫 SQL。**
"""
