# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tmp/
.tmp-mypy/
test-runtime/
.uv-cache/

# Virtual environments
.venv/
venv/
env/

# Local runtime data
.local-data/
.local-data-*/

# 研究抓取的第三方內容：可用 tools/fetch.py 依 manifest 重抓，不進版控。
# 追蹤的是「出處」而不是「內容」—— manifest.jsonl 有 URL、抓取時間與 SHA-256，
# 重抓後雜湊對得上就證明來源沒變、對不上就代表該來源已改版、結論需重新確認。
# 這樣既保有可重驗性，也不把別人的網頁內容轉載進本專案。
docs/research/sources/raw/
docs/research/sources/text/

# Build outputs
build/
dist/

# ============================================================
#  以下由 D:\Projects\_meta 於 2026-08-11 統一補上
#  補的是共用 base 的安全網，原有規則完全保留
#  規範見 D:\Projects\_meta\CONVENTIONS.md 第 6 節
# ============================================================

# SQLite 是本專案的主要儲存，資料庫檔一律不追蹤
*.db
*.db-journal
*.sqlite
*.sqlite3

.agents/
.claude/settings.local.json
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json
*.mp4
*.zip
*.7z
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# 法規參考庫的產生物 —— 隨時可以從 reference/corpus/ 重建。
# corpus 是手寫＋抓取的文字，**要進版控**；下面這些不進。
reference/reference.sqlite3
reference/raw/
